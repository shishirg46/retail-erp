// Frontend test setup: jest-dom matchers (toBeInTheDocument, toHaveTextContent,
// ...). globals: true in vitest.frontend.config.ts enables RTL's automatic
// cleanup between tests.

import "@testing-library/jest-dom/vitest";
